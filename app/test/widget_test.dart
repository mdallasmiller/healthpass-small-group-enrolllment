// Placeholder test. Real widget/integration tests land alongside the admin
// portal (M2) and employee portal (M5), with mocked Firebase where needed.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('sanity', () {
    expect(1 + 1, 2);
  });
}
